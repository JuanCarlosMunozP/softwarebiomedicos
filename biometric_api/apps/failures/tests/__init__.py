"""Pruebas de fallas (`apps.failures.tests`).

`test_api.py`: CRUD, acción `resolve`, recorte por área del
operativo y permisos. `test_models.py`: constraints
(resolved / resolved_at, fechas) e idempotencia de
`mark_resolved()`. Conftest y factories del equipo/usuario.
"""
